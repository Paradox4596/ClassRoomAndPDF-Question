1. 5개의 원소를 가진 다음 배열을 동적할당해 보세요.
{ 4, 8, 5, 3, 1 }
{ 1, 2, 3, 4, 5 }

2. 1번의 동적배열의 크기를 3개로 줄여 봅시다.
{ 4, 8, 5 }
{ 1, 2, 3 }

----------------------------------------------------------------------------

#include <iostream>

int main()
{
	int* pNumbers{ new int[5] {4, 8, 5, 3, 1} }; // new int[] 처럼 배열 크기 따로 안줘도 원래는 맞는 문법임! 그런데 실행 안되는건 비주얼 스튜디오 오류이다.

	int* pTemp{ new int[3] };

	for (int i = 0; i < 3; i++)
	{
		pTemp[i] = pNumbers[i];
	}

	delete pNumbers;

	pNumbers = pTemp;

	delete pTemp;
}

----------------------------------------------------------------------------
----------------------------------------------------------------------------

2차 배열 동적 메모리 할당

----------------------------------------------------------------------------
//처음부터 사실상 잘못만들어진 2차 배열

#include <iostream>

int main()
{
	int (*p)[3] = new int[2][3];

	p[0][0] = 0;

	// delete[] p; 가 되긴 하지만 올바른 표기는 아니다.
}

----------------------------------------------------------------------------
//제대로 만들어진 2차 배열 - 이 코드 반복적으로 잘 살피기

#include <iostream>

int main()
{
	int** array = new int* [2];

	for (int i = 0; i < 2; i++)
	{
		array[i] = new int[3];
	}

	array[0][0] = 1;
	array[0][1] = 2;
	array[0][2] = 3;

  for (int i = 0; i < 2; i++) // (2) 이렇게 뒤에 있는 얘들을 먼저 지워야 한다!
	{
		delete[] array[i];
	}


	delete array; // (1) 얘만 먼저 지우면 큰일 난다! int*가 가리키는 대상을 지울 방법이 없어지므로..
}

